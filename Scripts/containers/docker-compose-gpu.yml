services:

  sql-server:
    extends:
      file: docker-compose.sql-server.yml
      service: sql-server-2022
    networks:
      - backend

  # sql-sonarqube:
  #   extends:
  #     file: docker-compose.sonarqube.yml
  #     service: sonarqube
  #   networks:
  #     - backend

  # cvat-server:
  #   extends:
  #     file: docker-compose.cvat.yml
  #     service: cvat-server
  #   networks:
  #     - backend
      
  # cvat-ui:
  #   extends:
  #     file: docker-compose.cvat.yml
  #     service: cvat-ui
  #   networks:
  #     - backend

  apache-tika:
    extends:
      file: docker-compose.apache-tika.yml
      service: apache-tika
    networks:
      - backend

  label-studio:
    extends:
      file: docker-compose.label-studio.yml
      service: label-studio
    networks:
      - backend

  gotenberg:
    extends:
      file: docker-compose.gotenberg.yml
      service: gotenberg
    networks:
      - backend

  stirling-pdf:
    extends:
      file: docker-compose.stirling-pdf.yml
      service: stirling-pdf
    networks:
      - backend

  jupyterlab:
    extends:
      file: docker-compose.jupyterlab.yml
      service: jupyterlab
    networks:
      - backend
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge

volumes:
    sqlserver-2019-data:
    sqlserver-2022-data:
    sonarqube-data:
    sonarqube-extensions:
    sonarqube-logs:
    label-studio-data:
    stirling-pdf-trainingData:
    stirling-pdf-extraConfigs:
    jupyterlab-working:
    cvat-server-data:
